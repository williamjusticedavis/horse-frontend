// Augment bun:test's Matchers with @testing-library/jest-dom matchers.
// The package ships a dedicated bun type declaration but doesn't expose it
// via its exports map, so we reference the file directly.
/// <reference path="../../node_modules/@testing-library/jest-dom/types/bun.d.ts" />
